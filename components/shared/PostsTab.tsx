interface Props {
  currentUserId: string
  accountId: string
  accountType: string
}

const PostsTab = async ({ currentUserId, accountId, accountType }: Props) => {
  //TODO: Fetch profile posts
  return <section>Threads Tab</section>
}

export default PostsTab
